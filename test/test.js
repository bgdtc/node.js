let assert = require('assert');
const { db } = require("../server");

describe("MOCHA // CRUD // ARTICLE", () => {
    let article = {};

    beforeEach((done) => {
        let values = ["theo b", "theo72", "ema@il.mel", "motdepasse"]
    })
})